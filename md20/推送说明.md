# 推送到 GitHub 说明

## ✅ 当前状态

代码已经准备好上传：
- ✅ Git 仓库已初始化
- ✅ 代码已提交（3 个提交，68 个文件）
- ✅ 远程仓库已配置：`https://github.com/yeluoge26/caiwucuror.git`
- ✅ 敏感文件已排除（.gitignore）

## ⚠️ 重要：GitHub 不再支持密码认证

GitHub 从 2021 年 8 月起不再支持密码认证，必须使用 **Personal Access Token**。

## 📋 推送步骤

### 步骤1: 创建 Personal Access Token

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 设置：
   - **Note**: Finance System
   - **Expiration**: 90 days（或自定义）
   - **Select scopes**: 勾选 `repo`（全部权限）
4. 点击 "Generate token"
5. **复制 token**（只显示一次，请保存好！）

### 步骤2: 在 GitHub 创建仓库（如果还没有）

1. 访问：https://github.com/new
2. 设置：
   - **Repository name**: `caiwucuror`
   - **Description**: 财务管理系统
   - **Visibility**: Public 或 Private
   - **不要勾选** "Initialize with README"
3. 点击 "Create repository"

### 步骤3: 推送代码

#### 方式A: 使用批处理文件（推荐）

双击运行：`push_to_github.bat`

当提示输入密码时，**输入刚才复制的 token**（不是密码）

#### 方式B: 使用命令行

打开 PowerShell 或 CMD：

```bash
cd d:\code\caiwucuror
git push -u origin main
```

提示输入：
- **Username**: `yeluoge26`
- **Password**: 粘贴刚才复制的 token

## 🔒 安全提示

- ✅ Token 已添加到 `.gitignore`，不会上传
- ✅ 数据库配置文件 `app/config/db.php` 已排除
- ✅ 上传文件目录已排除

## 📝 如果推送失败

### 错误：仓库不存在
- 请先在 GitHub 创建仓库：https://github.com/new

### 错误：认证失败
- 确认使用的是 Token 而不是密码
- 确认 Token 有 `repo` 权限
- 确认 Token 未过期

### 错误：网络问题
- 检查网络连接
- 尝试使用代理或 VPN

## 🎯 推送成功后

访问仓库：https://github.com/yeluoge26/caiwucuror

## 💡 推荐：使用 SSH Key（更安全）

如果经常推送代码，建议使用 SSH Key：

1. **生成 SSH Key**:
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

2. **添加 SSH Key 到 GitHub**:
   - 复制 `~/.ssh/id_ed25519.pub` 内容
   - 访问：https://github.com/settings/keys
   - 点击 "New SSH key"，粘贴并保存

3. **修改远程 URL**:
   ```bash
   git remote set-url origin git@github.com:yeluoge26/caiwucuror.git
   git push -u origin main
   ```

---

**现在可以运行 `push_to_github.bat` 或执行 `git push -u origin main` 推送代码！**
